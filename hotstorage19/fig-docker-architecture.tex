\begin{figure}
	\centering
	% Requires \usepackage{graphicx}
	\includegraphics[width=0.48\textwidth]{graphs/fig-docker-architecture-v3}
	%\vspace{-15pt}
	\caption{Docker ecosystem. 
	%\LR{We should rename ``Docker Hub'' to ``Docker Registry''
	%to make it compatible with the text and be more general.}\NZ{how about docker ecosystem}
	%\VT{I think Lukas means the label in the cloud: it should be ``Docker Registry''.
	%Also, we need to adjust the Figure to show that Docker client is a software
	%component. Right now it looks like the client is a user (human).
	%Also we need to rename container layer to ``writable layer'' to be in sync
	%with the text.}
	}
	\label{fig-docker-architecture}
\end{figure}
