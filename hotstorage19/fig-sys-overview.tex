\begin{figure}[!t]
	\centering
	\subfigure[\sysname storage system
		\VT{Let's add the set of frontend registry servers in front of Sift cluster.}
		]{
		\includegraphics[width=0.4\textwidth]{graphs/sys-architecture.pdf} %width=\columnwidth
		\label{fig:sift}
	}
	\subfigure[\sysname registry storage cluster
	%\LR{We should remove the original registry in that
	    %figure as it has already been discussed in the background}
	]{
		\includegraphics[width=0.4\textwidth]{graphs/sift-arch-only.pdf}
		\label{fig:sift-original}
	}
	\caption{Architecture of \sysname. %\LR{The text in the figures is a bit too small. As a rule of thumb,
	%text in figures should be as large as normal text.}
	}
	\label{fig:sys-overview}
\end{figure}
